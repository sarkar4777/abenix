# Dev-only dependencies for ResolveAI's API test suite.
#
# Runtime deps live in requirements.txt; this file layers pytest and the
# bits the tests need on top. Install with:
#     pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
# httpx is already pinned in requirements.txt (0.27.2) and ships the
# ASGITransport we use to hit the FastAPI app in-memory — no need to
# repin here, just documenting the dependency.
freezegun==1.5.1
